# ===== General =====
# Port for the API service (Default: 4000)
PORT=4000

# Admin credentials for the dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_securely

# JWT Secret for authentication tokens
JWT_SECRET=super_secret_jwt_key_please_change

# ===== Database =====
# Path to the SQLite database file
DATABASE_URL=/data/sim.sqlite

# ===== WAHA (WhatsApp HTTP API) =====
# Base URL for the WAHA service (internal docker network address)
WAHA_BASE_URL=http://waha:3000

# API Key for WAHA (if enabled, currently optional for local but good practice)
WAHA_API_KEY=

# ===== Simulation Defaults =====
# Default messages per session per simulation run
SIM15_MESSAGES_PER_SESSION=30

# Daily message limit per session (safety cap)
DAILY_LIMIT_PER_SESSION=50

# ===== Frontend (Web) =====
# Internal URL for server-side API calls (internal docker network address)
API_INTERNAL_BASE_URL=http://api:4000

# Public URL for client-side API calls (browser access)
# For local development: http://localhost:4000/api
# For production (VPS with domain): https://sim.watrix.online/api
NEXT_PUBLIC_API_BASE_URL=https://sim.watrix.online/api
